# Baseline landing zone for managed VMs

The focus of this Azure landing zone is to be able to quickly on-board new VMs into a managed setup. The VMs can either be provisioned directly or brought into the landing zone as part of a migration.
