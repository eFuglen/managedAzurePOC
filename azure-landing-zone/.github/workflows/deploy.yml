name: Deploy Azure Landing Zone
# TODO: Define the workflow name and description

on:
    # TODO: Define triggers (push, pull_request, workflow_dispatch, etc.)
    workflow_dispatch:

# env:
    # TODO: Define environment variables
    # AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
    # AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
    # AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}

jobs:
    deploy:
        # TODO: Define job configuration
        runs-on: ubuntu-latest
        
        steps:
        - name: Checkout code
          uses: actions/checkout@v4
        
        # TODO: Add authentication steps
        # TODO: Add deployment steps
        # TODO: Add validation steps
        # TODO: Add notification steps